@page
@model PIM.Pages.DbTestes.DetalhesEscalonamentoModel
@{
 ViewData["Title"] = "Detalhes Escalonamento (Teste)";
}

<partial name="~/Pages/Shared/_DbTestesNav.cshtml" />

<div class="alert alert-info">Guia: Mostra detalhes de um escalonamento. Use ?id= na URL para testar com um id.</div>

<h2>Detalhes do Escalonamento</h2>

@if (Model.Message != null)
{
 <div class="alert alert-info">@Model.Message</div>
}
else
{
 <p>Detalhes exibidos (simulado).</p>
}